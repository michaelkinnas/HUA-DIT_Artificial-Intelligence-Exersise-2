
    # while (not problem.isFinalState(currentState)):
        
    #     if problem.getNextStates(currentState) > 0:
    #         currentState = stack.dequeue()
            

    #         for nextState in problem.getNextStates(currentState):
    #             stack.enqueue(nextState)
        